spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/yearend?useSSL=false&serverTimezone=Hongkong&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    mvc:
      hiddenmethod:
        filter:
          enabled: true
server:
  port: 8080
